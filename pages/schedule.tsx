import Layout from "../components/Layout";

const Schedule = () => {
    return (
        <Layout>
            <div className="w-4/5 max-w-6xl mx-auto pt-40 flex justify-center">
                Hey! You&apos;re early, we&apos;ll release the schedule closer
                to the event!
            </div>
        </Layout>
    );
};

export default Schedule;
